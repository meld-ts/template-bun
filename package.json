{
  "name": "@meld-ts/create-bun",
  "description": "A modern Bun project template with TypeScript, Biome, and first-class support for lib, app, and react project modes.",
  "version": "1.0.8",
  "type": "module",
  "license": "MIT",
  "keywords": ["bun", "template", "typescript", "biome", "react"],
  "repository": {
    "type": "git",
    "url": "https://github.com/meld-ts/create-bun.git"
  },
  "bin": {
    "create-bun": "index.js"
  },
  "files": ["index.js", "template-lib", "template-app", "template-react-app"],
  "scripts": {
    "build": "bun run src/build.ts",
    "prepublishOnly": "bun run build",
    "fmt": "biome format --write ./src",
    "lint": "biome lint --error-on-warnings ./src",
    "ts-check": "tsgo --noEmit",
    "biome-migrate": "bunx biome migrate --write",
    "dev:lib": "bun src/index.ts test-lib --mode lib --cwd ./temp --rm --template-root ./",
    "dev:app": "bun src/index.ts test-app --mode app --cwd ./temp --rm --template-root ./",
    "dev:react": "bun src/index.ts test-app --mode react --cwd ./temp --rm --template-root ./"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest",
    "@typescript/native-preview": "latest"
  }
}
